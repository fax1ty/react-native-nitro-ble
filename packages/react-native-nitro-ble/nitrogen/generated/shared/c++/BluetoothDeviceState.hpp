///
/// BluetoothDeviceState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::bluetooth {

  /**
   * An enum which can be represented as a JavaScript union (BluetoothDeviceState).
   */
  enum class BluetoothDeviceState {
    DISCONNECTED      SWIFT_NAME(disconnected) = 0,
    CONNECTED      SWIFT_NAME(connected) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::bluetooth

namespace margelo::nitro {

  // C++ BluetoothDeviceState <> JS BluetoothDeviceState (union)
  template <>
  struct JSIConverter<margelo::nitro::bluetooth::BluetoothDeviceState> final {
    static inline margelo::nitro::bluetooth::BluetoothDeviceState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("disconnected"): return margelo::nitro::bluetooth::BluetoothDeviceState::DISCONNECTED;
        case hashString("connected"): return margelo::nitro::bluetooth::BluetoothDeviceState::CONNECTED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum BluetoothDeviceState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::bluetooth::BluetoothDeviceState arg) {
      switch (arg) {
        case margelo::nitro::bluetooth::BluetoothDeviceState::DISCONNECTED: return JSIConverter<std::string>::toJSI(runtime, "disconnected");
        case margelo::nitro::bluetooth::BluetoothDeviceState::CONNECTED: return JSIConverter<std::string>::toJSI(runtime, "connected");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert BluetoothDeviceState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("disconnected"):
        case hashString("connected"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
